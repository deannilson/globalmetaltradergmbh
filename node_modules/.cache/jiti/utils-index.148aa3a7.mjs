"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.$products = exports.$locales = exports.$app = void 0;exports.generateSlug = generateSlug;var _categories = _interopRequireDefault(await jitiImport("../resources/categories.json"));function _interopRequireDefault(e) {return e && e.__esModule ? e : { default: e };}
const $app = exports.$app = {
  name: "Global Metal Trader GmbH",
  desc: "Global Metal Trader GmbH is a leading scrap metal supplier and exporter, specializing in the purchase and sale of ferrous and non-ferrous metals. Based in Germany, we deliver reliable recycling solutions worldwide.",
  domain: "www.globalmetaltradergmbh.com",
  email: "sales@globalmetaltradergmbh.com",
  phone: "+49 4321 8890",
  address: "Schönbühlstr. 46  <br />70188 Stuttgart, Germany",
  hours: "Mon-Fri: 7:00am-6:00pm",
  vat: "DE 317082838",
  reg: "HRB 763998",
  contact: "Vijaypal Kishorilal",
  api: "https://xtremnet.vercel.app/mailer"
};
function generateSlug(str) {
  return str.
  toLowerCase() // Convert to lowercase
  .replace(/[^a-z0-9\s-]/g, " ") // Replace non-alphanumeric with space
  .trim() // Remove leading/trailing spaces
  .replace(/\s+/g, "-") // Replace spaces with hyphens
  .replace(/-+/g, "-"); // Replace multiple hyphens with a single hyphen
}
const $products = exports.$products = _categories.default.flatMap(({ slug, products }) =>
products.map((name) => ({
  slug: generateSlug(name),
  name,
  cat: slug,
  image: "",
  desc: "",
  specs: ""
}))
);

const $locales = exports.$locales = Object.fromEntries(
  _categories.default.map(({ slug, name }) => [slug, name])
); /* v9-f430c311df291b11 */
